export interface ILoadAudioBufferResult {
    audioBuffer: AudioBuffer;
    fileSize: number;
}
/**
 * Loads and decodes an audio-file, resulting in an AudioBuffer and the fileSize of the loaded file.
 * @param {AudioContext} context
 * @param {string} url
 * @param {(value: number) => void} onProgress
 * @returns {Promise<ILoadAudioBufferResult>}
 */
export declare function loadAudioBuffer(context: AudioContext, url: string, onProgress?: (value: number) => void): Promise<ILoadAudioBufferResult>;
